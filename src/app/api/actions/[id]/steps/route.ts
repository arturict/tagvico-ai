import { revalidatePath } from 'next/cache'; import { assertCanMutateWorkspace, assertSameOrigin, apiError, readJsonBody, requireApiUser } from '@/lib/server/auth'; import { actionCenter, workspaceFor } from '@/lib/server/workspace';
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { try { await assertSameOrigin(request); const user = await requireApiUser(); const workspace = workspaceFor(user); assertCanMutateWorkspace(workspace.role); const { id } = await params; const item = actionCenter.addStep(workspace.householdId, id, workspace.memberId, await readJsonBody(request)); revalidatePath(`/actions/${id}`); return Response.json(item, { status: 201 }); } catch (error) { return apiError(error); } }
