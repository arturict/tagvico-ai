import { revalidatePath } from 'next/cache'; import { assertCanMutateWorkspace, assertSameOrigin, apiError, readJsonBody, requireApiUser } from '@/lib/server/auth'; import { actionCenter, workspaceFor } from '@/lib/server/workspace'; const sync = require('../../../../../services/actionSyncService') as typeof import('../../../../../services/actionSyncService');
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { await assertSameOrigin(request); const user = await requireApiUser(); const workspace = workspaceFor(user); assertCanMutateWorkspace(workspace.role); const { id } = await params; actionCenter.updateCase(workspace.householdId, id, workspace.memberId, await readJsonBody(request)); try { await sync.pushCase(workspace.householdId, id, workspace.memberId); } catch { /* sync error is persisted on the case */ } const item = actionCenter.getCase(workspace.householdId, id); revalidatePath(`/actions/${id}`); return Response.json(item); } catch (error) { return apiError(error); } }
